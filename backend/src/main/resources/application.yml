spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/money_manager_db
    username: postgres
    password: Pawan@18
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect